services:
  gocart:
    build:
      context: ./
      dockerfile: dockerfile
    container_name: go-cart
    ports:
      - "3001:3000"
    restart: always

    networks:
      - app-network

networks:
  app-network:
    external: true
